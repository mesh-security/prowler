apiVersion: v1
kind: Service
metadata:
  name: {{ include "prowler.fullname" . }}
  labels:
    {{- include "prowler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prowler.selectorLabels" . | nindent 4 }}
